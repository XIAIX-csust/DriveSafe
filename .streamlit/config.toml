# DriveSafe 主题配置
#
# 字体统一在这里声明（Google Fonts），不要在自定义 CSS 里另立一套 ——
# 之前 app.py 顶部 @import 了 Orbitron / Rajdhani / Share Tech Mono，
# 现在统一改为：
#   UI / 正文 / 标题：Inter        （当前最主流的现代 UI 字体）
#   等宽与数字读数    ：JetBrains Mono
#
# 说明：改动本文件后刷新页面即可生效；若改了 fontFaces（自托管字体）需重启服务。

[theme]
font = "Inter:https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
headingFont = "Inter:https://fonts.googleapis.com/css2?family=Inter:wght@500;600;700;800&display=swap"
codeFont = "'JetBrains Mono':https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap"

baseFontWeight = 400
codeFontWeight = 400
headingFontWeights = [700, 600, 600, 600, 600, 600]
